@model AspNetMvc_WeChat_Base.Model.WeChatMediaUpload
@{
    ViewBag.Title = "【例8-03】新增永久素材封面 - Chapter08";
}

<div class="jumbotron">
    <h2>【例8-03】新增永久素材封面</h2>
</div>
<div class="jumbotron">
    @ViewBag.mediaThumbCoverID
    <br />
    @using (Html.BeginForm("MediaPermanentCover", "Home", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        @Html.AntiForgeryToken()
        <div class="form-horizontal">
            <div class="form-group">
                @Html.Label("选择永久封面素材:", new { @class = "control-label col-md-2" })
                <div class="col-md-3">
                    <input type="file" class="form-control-file" name="uploadFile" id="uploadFile" />
                </div>
            </div>
            <div class="form-group">
                <div class="col-md-3 col-md-offset-2">
                    <input type="submit" name="uploadSubmit" value="上传" class="btn btn-default" />
                </div>
            </div>
        </div>
    }
</div>

<div>
    @Html.ActionLink("返回列表", "index")
</div>